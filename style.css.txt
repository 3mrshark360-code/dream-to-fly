/* Hero Section */
.hero {
  background: linear-gradient(to right, #0b5ed7, #2a9df4);
  color: white;
  text-align: center;
  padding: 80px 20px;
  border-radius: 10px;
}

.hero h1 {
  font-size: 2.5em;
  margin-bottom: 20px;
}

.hero p {
  font-size: 1.2em;
  margin-bottom: 30px;
}

/* WhatsApp Button Style */
.whatsapp-btn {
  display: inline-block;
  background-color: #25D366;
  color: white;
  padding: 14px 28px;
  border-radius: 50px;
  text-decoration: none;
  font-weight: bold;
  transition: background 0.3s, transform 0.2s;
  font-size: 1em;
}

.whatsapp-btn:hover {
  background-color: #1ebe57;
  transform: scale(1.05);
}

/* Responsive */
@media (max-width: 768px) {
  .hero h1 {
    font-size: 2em;
  }
  .hero p {
    font-size: 1em;
  }
  .whatsapp-btn {
    padding: 12px 20px;
    font-size: 0.9em;
  }
}